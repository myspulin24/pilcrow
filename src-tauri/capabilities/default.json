{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Reader_MJ běží celý místně. Potřebuje číst a zapisovat složku trezoru, ukázat výběr složky pro import/export a ukázat soubory ve správci souborů. Jediné síťové spojení navazuje updater, a to výhradně na GitHub Releases; o nic dalšího si neříká -- žádný shell, žádná oznámení.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "dialog:allow-open",
    "opener:allow-reveal-item-in-dir",
    "updater:default",
    "process:allow-restart"
  ]
}
